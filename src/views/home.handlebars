<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4">
                <i class="fas fa-boxes text-primary"></i> 
                Productos Disponibles
            </h1>
            <div>
                <span class="badge bg-primary fs-6">Total: {{products.length}} productos</span>
            </div>
        </div>

        {{#if products.length}}
            <div class="row">
                {{#each products}}
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card product-card h-100 shadow-sm">
                        <div class="card-header bg-light">
                            <h5 class="card-title mb-0">
                                <i class="fas fa-tag text-primary"></i>
                                {{title}}
                            </h5>
                        </div>
                        <div class="card-body d-flex flex-column">
                            <div class="mb-3">
                                <img src="{{thumbnail}}" 
                                     alt="{{title}}" 
                                     class="img-fluid rounded"
                                     style="max-height: 200px; object-fit: cover; width: 100%;"
                                     onerror="this.src='https://via.placeholder.com/300x200?text=Sin+Imagen'">
                            </div>
                            
                            <p class="card-text text-muted flex-grow-1">
                                {{description}}
                            </p>
                            
                            <div class="mt-auto">
                                <div class="row text-center mb-3">
                                    <div class="col-6">
                                        <small class="text-muted">Precio</small>
                                        <div class="h5 text-success mb-0">${{price}}</div>
                                    </div>
                                    <div class="col-6">
                                        <small class="text-muted">Stock</small>
                                        <div class="h5 text-info mb-0">{{stock}}</div>
                                    </div>
                                </div>
                                
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge bg-secondary">{{category}}</span>
                                    <small class="text-muted">ID: {{id}}</small>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-transparent">
                            <div class="d-flex justify-content-between">
                                <span class="text-muted">
                                    <i class="fas fa-check-circle text-success"></i>
                                    Disponible
                                </span>
                                <button class="btn btn-outline-primary btn-sm" 
                                        onclick="addToCart('{{id}}')">
                                    <i class="fas fa-cart-plus"></i> Agregar
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        {{else}}
            <div class="text-center py-5">
                <i class="fas fa-box-open fa-5x text-muted mb-4"></i>
                <h3 class="text-muted">No hay productos disponibles</h3>
                <p class="text-muted">Los productos aparecerán aquí cuando estén disponibles.</p>
                <a href="/realtimeproducts" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Agregar Productos
                </a>
            </div>
        {{/if}}
    </div>
</div>
